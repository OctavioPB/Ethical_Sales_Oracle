apiVersion: v1
kind: Service
metadata:
  name: eso-dashboard
  namespace: eso-production
  labels:
    app: eso-dashboard
spec:
  selector:
    app: eso-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
