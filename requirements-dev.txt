# ESO dev dependencies — install inside a virtualenv, not globally.
# Setup: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements-dev.txt
# After install: python -m spacy download en_core_web_sm

pytest>=8.2,<10
pytest-mock>=3.14
pytest-cov>=5.0
spacy>=3.7,<4
pyyaml>=6.0
anthropic>=0.40
kafka-python>=2.0
psycopg2-binary>=2.9
# safety is CI-only — pinned in .github/workflows/security.yml, not needed locally
