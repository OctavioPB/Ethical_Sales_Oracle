apiVersion: v1
kind: Service
metadata:
  name: eso-api
  namespace: eso-production
  labels:
    app: eso-api
spec:
  selector:
    app: eso-api
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
